'use strict';

const insert = require('../insertion/index');

describe('Insertion Sort Test', () => {
  it('Can Loop though an array and sort', () => {
  });

  it('can insert and rearrange the array given.', () => {
  });
});
