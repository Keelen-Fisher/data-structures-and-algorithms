'use strict';

let mergeSort = require('../merge/index');

describe('Merge Sort Test', () => {
  it('Can Loop though an array and sort', () => {
  });

  it('can check the array on the left and right side.', () => {
  });

  it('can split the array.', () => {
  });

  it('merge the array after splitting the original array.', () => {
  });
});
